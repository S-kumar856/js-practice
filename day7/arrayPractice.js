// Task 1: Create an array of 5 colors using each method of array creation
// type 1: Creating array using array literal

// here "colors" is the array name and inside the square bracket we are adding the colors elements
// colors  is variable name and it can be anything and every element inside the square bracket assigned a index number starting from 0

const colors = ['red', 'skyblue', 'green', 'yellow', 'black'];


// type 2: Creating empty array first, then adding elements using the square bracket notation with index number
const colors2 = [];
colors2[0] = 'red';
colors2[1] = 'skyblue';
colors2[2] = 'green';
colors2[3] = 'yellow';
colors2[4] = 'black';


// Task 2: Create an array of 5 numbers
// type 1: Creating array using array 
// here "numbers" is the array name and inside the square bracket we are adding the numbers elements
const numbers = [1, 2, 3, 4, 5];

// type 2: Creating empty array first, then adding elements using the square bracket notation with index number
const numbers2 = [];
numbers2[0] = 1;
numbers2[1] = 2;
numbers2[2] = 3;
numbers2[3] = 4;
numbers2[4] = 5;

// Task 3: Printing all arrays using console.log()

// Task 1 outputs printing
console.log(colors[0]) //output: red
console.log(colors[1]) //output: skyblue
console.log(colors) //output: [ 'red', 'skyblue', 'green', 'yellow', 'black' ]

console.log(colors2[0]) //output: red
console.log(colors2[1]) //output: skyblue
console.log(colors2) //output: [ 'red', 'skyblue', 'green', 'yellow', 'black' ]
